name: 01 - Building blocks

on: push
  
jobs:
     echo-Hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "hello world!"
     echo-goodbye:
          runs-on: ubuntu-latest
          steps:
              - name: failed step
                run:  |
                  echo  "You will fail"
                  exit 1
              - name: Say goodbye
                run: echo "Goodbye!"
